package com.unibuc.designpatterns.behavioral.observer;

public interface Observer {

    void display(int totalFirstServeWins,
                 int latestServeSpeed,
                 int totalPointPlayedOnFirstServe);
}
